#include "main.h"
#include <stdio.h>
/**
 * add - Description: adds two integers
 * Return: a result
 * @int: parameter
 * @int: parameter
 */
int add(int, int)
{
	int result = add(num1, num2);

	return (result);
}
